<section class="app-detail-hero">
  <div class="container-fluid px-lg-5 py-5">
    <div class="row g-4 align-items-center">
      <div class="col-xl-7">
        <p class="eyebrow">{{exampleApp.eyebrow}}</p>
        <h1>{{exampleApp.title}}</h1>
        <p class="hero-copy">{{exampleApp.summary}}</p>
      </div>
      <div class="col-xl-5">
        <div class="surface-card app-access-card">
          <span class="tile-icon"><i class="bi {{exampleApp.icon}}"></i></span>
          <h2>Access this workflow</h2>
          <p>{{exampleApp.detail}}</p>
          {{#if isAuthenticated}}
            <a class="btn btn-primary w-100" href="{{exampleApp.path}}">Open {{exampleApp.title}}</a>
          {{else}}
            {{!--
              One button, because there is one way in. The dropdown behind it
              offered Verified ID, YubiKey and "wallet-only lab" — two methods
              that are not wired into this application, and a third whose name
              described the plumbing rather than what the person does. Signing
              in is scanning a code with the wallet; the picker is on the Aegis
              side, where the sign-in methods are actually configured.
            --}}
            <form action="/auth/start" method="post">
              <input type="hidden" name="intent" value="sign-in">
              <input type="hidden" name="authMethod" value="wallet">
              <input type="hidden" name="returnTo" value="{{exampleApp.path}}">
              <button class="btn btn-primary w-100" type="submit">
                <i class="bi bi-qr-code-scan me-2"></i>Sign in with your wallet
              </button>
            </form>
            <p class="small text-muted mt-2 mb-0">
              You will scan a code with the Aegis ID wallet. Nothing to type, and no password.
            </p>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</section>

<section class="container-fluid px-lg-5 py-5">
  <div class="section-heading compact">
    <p class="eyebrow">Workflow</p>
    <h2>What this demonstrates.</h2>
  </div>
  <div class="workflow-grid">
    {{#each exampleApp.steps}}
      <article class="workflow-step">
        <span>{{stepNumber @index}}</span>
        <p>{{this}}</p>
      </article>
    {{/each}}
  </div>
</section>
