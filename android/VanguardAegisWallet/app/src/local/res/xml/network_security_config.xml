<?xml version="1.0" encoding="utf-8"?>
<!--
  The local flavour only. Android blocks cleartext HTTP from API 28, so talking
  to a development server on the host machine needs an explicit exemption.
  Scoped to the emulator's host alias and loopback rather than opened globally,
  and it never reaches dev, qa or prod because it lives in src/local.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
